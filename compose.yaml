services:
  web:
    build:
      context: .
    container_name: sine-web
    environment:
      SESSION_SECRET: ${SESSION_SECRET:-dev-session-secret}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
